-r requirements.txt

# Testing
pytest==8.3.3
pytest-cov==6.0.0

# Optional property-based tests (handy for numeric functions)
hypothesis==6.118.7

# Linting (if you want tests to run only; CI linting can be separate)
ruff==0.7.2
